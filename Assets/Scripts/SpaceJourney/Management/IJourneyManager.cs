using Interfaces;

namespace SpaceJourney.Management
{
    public interface IJourneyManager : IManager
    {
        /*IChaptersConfigData ChaptersConfig { get; }
        JourneySaveData JourneySaves { get; }
        ChapterProgress GetCurrentChapter();
        /// <summary>
        /// Call it to get visual config APPYING that it could be treasure/tiki/etc
        /// </summary>
        /// <param name="onLoaded"></param>
        /// <returns></returns>
        ChapterVisualsConfig GetVisualConfigAplyable(UnityAction<ChapterVisualsConfig> onLoaded = null);
        /// <summary>
        /// Call it to get visual config for ONLY current chapter
        /// </summary>
        /// <param name="onLoaded"></param>
        /// <returns></returns>
        ChapterVisualsConfig GetVisualConfigChapter(UnityAction<ChapterVisualsConfig> onLoaded = null);*/
    }
}